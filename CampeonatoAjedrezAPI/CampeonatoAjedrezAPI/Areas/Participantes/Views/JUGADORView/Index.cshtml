@model IEnumerable<CampeonatoAjedrezAPI.Areas.JUGADOR>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PARTICIPANTE.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NIVEL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PARTICIPANTE.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NIVEL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDJUGADOR }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDJUGADOR }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDJUGADOR })
        </td>
    </tr>
}

</table>
