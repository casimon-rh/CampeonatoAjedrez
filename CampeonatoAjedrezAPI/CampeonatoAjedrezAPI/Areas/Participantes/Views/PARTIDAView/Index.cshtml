@model IEnumerable<CampeonatoAjedrezAPI.Areas.PARTIDA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ARBITRO.IDARBITRO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ARBITRO.IDARBITRO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDPARTIDA }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDPARTIDA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDPARTIDA })
        </td>
    </tr>
}

</table>
